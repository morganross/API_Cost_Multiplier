input_folder: ./test/mdinputs
output_folder: ./test/mdoutputs
instructions_file: ./test/instructions.txt
one_file_only: true

# Default number of iterations to use when a per-group value is not provided
iterations_default: 1

# Per-group iteration counts (override iterations_default as needed)
iterations:
  ma: 
  gptr:
  dr: 
  fpf: 
