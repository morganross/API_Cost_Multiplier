openrouter:
  default:
    provider_name: openrouter
    model_name: default
    api_params:
      max_tokens: max_tokens
    context_window: 8192
    quirks:
    - Can encounter 'max tokens exceeds max context length' issues.
    - Context overflows can occur with large inputs if not managed.
    api_version_compatibility: latest
